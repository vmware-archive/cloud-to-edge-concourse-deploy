- name: Add some useful command line utilities
  hosts: bootstrap
  become: True
  vars:
    nginx_sites:
      default:
        - listen 81
        - server_name _
        - root "/home/vmware/deployroot/skyway-deploy/downloads"
        - autoindex on
  roles:
    - nginx
